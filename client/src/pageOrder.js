// Top-level pages, in the order swiping left/right (or the arrow buttons) moves through them.
// Sub-routes like /blog/:slug aren't part of the swipe sequence.
export const PAGE_ORDER = ['/', '/achievements', '/blog', '/books', '/skate-forward'];
